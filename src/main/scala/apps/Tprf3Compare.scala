package apps

import uni.data.*
import uni.data.Mat.*
import uni.*

/**
 * Cross-validates Tprf3.scala against py/tprf3.py by reading common CSV
 * fixtures generated by py/gen_tprf3_fixtures.py.
 *
 * Run:  sbt "runMain apps.Tprf3Compare"
 */
object Tprf3Compare {

  /** Absolute max difference between two column vectors, ignoring NaN pairs. */
  private def maxAbsDiff(a: MatD, b: MatD): Double =
    var worst = 0.0
    for i <- 0 until a.rows do
      val av = a(i, 0); val bv = b(i, 0)
      if !av.isNaN && !bv.isNaN then
        worst = math.max(worst, math.abs(av - bv))
    worst

  private def check(label: String, got: MatD, ref: MatD, tol: Double = 1e-8): Unit =
    val diff = maxAbsDiff(got, ref)
    val status = if diff <= tol then "PASS" else "FAIL"
    printf("%-22s  %s  max|Δ|=%.3e%n", label, status, diff)
    if diff > tol then
      // Print first few differing rows for diagnosis
      var shown = 0
      for i <- 0 until got.rows if shown < 5 do
        val av = got(i, 0); val bv = ref(i, 0)
        if !av.isNaN && !bv.isNaN && math.abs(av - bv) > tol then
          printf("  row %3d: scala=%.10f  python=%.10f  diff=%.3e%n", i, av, bv, math.abs(av - bv))
          shown += 1

  def main(args: Array[String]): Unit =
    // Locate test-data dir relative to the project root
    val rootDir = sys.props.getOrElse("user.dir", ".")
    val dataDir = s"$rootDir/test-data/tprf3"

    def load(name: String): MatD =
      val p = s"$dataDir/$name".path
      require(p.isFile, s"Missing fixture: ${p.posx}  (run py/gen_tprf3_fixtures.py first)")
      p.loadMatD

    println(s"Loading fixtures from $dataDir ...")
    val y = load("y.csv")
    val X = load("X.csv")
    val Z = load("Z.csv")
    val T = y.rows

    println(s"  y: ${y.rows}×${y.cols}  X: ${X.rows}×${X.cols}  Z: ${Z.rows}×${Z.cols}")
    println()

    // ── IS Full (explicit Z) ─────────────────────────────────────────────────
    val (fore_is, pt_is, _) = Tprf3.estimate3prf(y, X, Right(Z), procedure = "IS Full")
    val ref_is = load("fore_is.csv")
    printf("IS Full        R²=%.6f  fore[0]=%.10f%n", pt_is.rsquare, fore_is(0, 0))
    check("IS Full", fore_is, ref_is)

    // ── IS Full (autoproxy L=2) ──────────────────────────────────────────────
    val (fore_ia, pt_ia, _) = Tprf3.estimate3prf(y, X, Left(2), procedure = "IS Full")
    val ref_ia = load("fore_is_auto.csv")
    printf("IS Full auto   R²=%.6f  fore[0]=%.10f%n", pt_ia.rsquare, fore_ia(0, 0))
    check("IS Full auto", fore_ia, ref_ia)

    // ── OOS Recursive ────────────────────────────────────────────────────────
    val (fore_or, pt_or, _) = Tprf3.estimate3prf(y, X, Right(Z), procedure = "OOS Recursive",
                               mintrain = (50, 0))
    val ref_or = load("fore_oos_rec.csv")
    val n_or = (0 until T).count(i => !fore_or(i, 0).isNaN)
    printf("OOS Recursive  R²=%.6f  n_valid=%d%n", pt_or.rsquare, n_or)
    check("OOS Recursive", fore_or, ref_or)

    // ── OOS Cross Val ────────────────────────────────────────────────────────
    val (fore_cv, pt_cv, _) = Tprf3.estimate3prf(y, X, Right(Z), procedure = "OOS Cross Val")
    val ref_cv = load("fore_oos_cv.csv")
    val n_cv = (0 until T).count(i => !fore_cv(i, 0).isNaN)
    printf("OOS Cross Val  R²=%.6f  n_valid=%d%n", pt_cv.rsquare, n_cv)
    check("OOS Cross Val", fore_cv, ref_cv)

    // ── Tprf3.tprfFast ────────────────────────────────────────────────────────
    println()
    println("── Tprf3 IS Full: tprfFast ──────────────────────────────────────────────")
    val result = Tprf3.tprfFast(X, y, Z)
    printf("fast           R²=%.6f  yhat[0]=%.10f  adjR²=%.6f%n",
      TprfFromBreeze.rr2(y, result.y_hat), result.y_hat(0, 0), result.adjRsq)
    check("fast vs Python ref", result.y_hat, ref_is)

    // ── Factor matrix ─────────────────────────────────────────────────────────
    println()
    val p1r2 = result.pass1columnsRsquared
    printf("phi: %d×%d  sigma: %d×%d  pass1R²[0]=%.4f%n",
      result.phi.rows, result.phi.cols, result.sigma.rows, result.sigma.cols,
      if p1r2.nonEmpty then p1r2(0) else Double.NaN)

    println()
    println("Done.")
}
